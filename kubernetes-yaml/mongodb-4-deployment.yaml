apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: mongodb-4
  name: mongodb-4
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: mongodb-4
    spec:
      containers:
      - args:
        - mongod
        - --shardsvr
        - --replSet
        - mongodbshard-2
        image: mongo:3
        name: mongodb-4
        resources:
          limits:
            memory: 9Gi
        volumeMounts:
        - mountPath: /data/db
          name: mongodb-4-disk
      restartPolicy: Always
      volumes:
      - name: mongodb-4-disk
        gcePersistentDisk:
          pdName: mongodb-4
          fsType: ext4
status: {}
